services:
  postgres:
    image: postgres:18-alpine
    restart: always
    environment:
      POSTGRES_USER: monqom
      POSTGRES_PASSWORD: monqom
      POSTGRES_DB: monqom_dev
    ports:
      - "5432:5432"
    volumes:
      - monqom-pgdata:/var/lib/postgresql/data

volumes:
  monqom-pgdata:
